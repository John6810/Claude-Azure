# Check Naming Conventions

Verify naming conventions for `$ARGUMENTS` (file or folder).

Invoke `naming-checker` agent on the target path. It holds the single source of truth for all naming rules (prefixes, patterns, constraints, Entra ID groups).
