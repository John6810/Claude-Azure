# Generate Module Documentation

Generate README.md for module `$ARGUMENTS`. Use `all` for bulk generation + global index.

Invoke `documentation` agent which reads module source files and generates standardized README with inputs, outputs, and usage examples.
