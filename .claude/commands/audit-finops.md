# FinOps Audit

Run a FinOps audit on `$ARGUMENTS`.

Invoke `finops` agent on the target workload. It checks: mandatory tags, expensive resources (DDoS, AKS, ACR, Palo Alto), LAW retention, orphaned resources, optimization opportunities.
