# Security Audit

Run a full security audit on `$ARGUMENTS`.

Invoke `security-reviewer` agent on the target workload. It checks: RBAC/PIM matrix, Private Endpoints, Managed Identities, Key Vault config, network exposure, tag compliance.
